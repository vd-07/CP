/* Authored by Vivek Dubey */

#include <iostream>

using namespace std;

int main(){
	// int n, q;
	// cin>>n>>q;
	// int a[n];
	// for(int i=0;i<n;i++)
	// 	cin>>a[i];
	// // int prefix[n];

	// // //building up the prefix array for counting the continuous numbers
	// // //for increasing numbers starting before or from L and ending after or at R

	// // prefix[0]=0;
	// // for(int i=1;i<n;i++){
	// // 	if(a[i]>a[i-1])
	// // 		{
	// // 			prefix[i]=prefix[i-1]+1;
	// // 			i++;
	// // 			while(a[i]>a[i-1])
	// // 				{
	// // 					prefix[i]=prefix[(i)-1];
	// // 					i++;
	// // 				}
	// // 		}
	// // 		prefix[i]=prefix[i-1];
	// // }
	// // for(auto s:prefix){
	// // 	cout<<s<<" ";
	// // }
	// // cout<<endl;
	// int l, r;
	// while(q--){
	// 	cin>>l>>r;
	// 	if((a[l]<a[l-1]&&a[r-1]>a[r-2])||(a[l]>a[l-1]&&a[r-1]<a[r-2]))
	// 		cout<<"YES\n";
	// 	else
	// 		cout<<"NO\n";

	// }
	cout<<"adarsh bhsodika randi ka bacha hai saala bhen ka loda"<<endl;
	return 0;
}